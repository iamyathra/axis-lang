// Not a real extension - just a fixture file for modules.test.js to check
// resolveModules records its path correctly. Never actually loaded/run by
// any test in this file (that's tests/browser/*.test.js's job).
export function registerNothing() {}
